@model Aiwins.Rocket.AspNetCore.Mvc.UI.Widgets.Components.WidgetResourcesViewModel
@if (Model.Widgets.Any())
{
    var resourceItems = Model.Widgets.SelectMany(w => w.Styles).ToArray();
    if (resourceItems.Any())
    {
        <rocket-style-bundle>
            @foreach (var resourceItem in resourceItems)
            {
                if (resourceItem.Src != null)
                {
                    <rocket-style src="@resourceItem.Src" />
                }
                else
                {
                    <rocket-style type="@resourceItem.Type" />
                }
            }
        </rocket-style-bundle>
    }
}